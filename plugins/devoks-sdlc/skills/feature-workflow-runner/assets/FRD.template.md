---
doc_type: frd
id: FRD-<feature-slug>
title: <기능 이름>
status: draft        # draft | review | approved | done
owner: <이름>
updated: <YYYY-MM-DD>
plan: ./PLAN.md
issue:              # 선택 — 연결 GitHub 이슈 URL 또는 번호 (없으면 생략)
---

# [<기능 이름>] Feature Requirement Document

> 작은 기능용 통합 문서. PRD(왜) + TRD 일부(어떻게) + FeatureSpec(무엇을)을 한 곳에.
> 흩어지기 쉬운 **Contract**(파라미터/상태전이)와 **Resource**(시안/코드/세팅 키)는
> §5·§6에 모아 착수 전 한눈에 보도록 한다. 작업 분해는 `PLAN.md`로 위임.

## 1. Goal
<무엇을, 왜 — 1~2문장>

## 2. Context
- **현재 상황(as-is):** <문제 / 결함 / 공백>
- **관련 기능:** <연관 화면 / 모듈 / 시스템>

## 3. Requirements (EARS)
> Acceptance Criteria는 `WHEN/IF/WHILE ... THE SYSTEM SHALL ...` 로. 테스트로 변환 가능하게.
> 작성법 상세: `references/ears-acceptance-criteria.md`

### REQ-001: <요구사항 제목>
- **동작:** <시스템이 해야 하는 일>
- **사용자 흐름:** <진입점 → 단계>
- **AC:**
  - `AC-001-1` WHEN <트리거> THE SYSTEM SHALL <동작>
  - `AC-001-2` IF <미통과/예외> THEN THE SYSTEM SHALL <재시도/폴백>

### REQ-002: <요구사항 제목>
- **동작:** ...
- **AC:**
  - `AC-002-1` WHEN ... THE SYSTEM SHALL ...

## 4. Design Spec
> 무엇을(요구)에서 한 발 더 들어간 **어떻게(구조)**. 컴포넌트·패턴·모듈 배치를 착수 전에 고정해
> PLAN의 Task 분해가 임의 구조로 흩어지지 않게 한다.
> **복잡도가 낮으면 4.2~4.4는 1~2줄로 축약**해도 된다. 복잡도 임계(파일 3개 초과 · 신규 모듈/계층 ·
> 아키텍처 변경 · 새 디자인 패턴 도입)를 넘으면 에이전트가 **설계안을 먼저 제안 → 확인** 후 채운다.
> 작성법·DSN 규칙·제안 절차 상세: `references/design-spec.md`

### 4.1 화면/흐름
- **화면/흐름:** <화면1 → 화면2 → ...> (라우팅·네비게이션 단위 명시)
- **조건부 렌더링/분기:** <결과 화면 등 조건 분기>
- **시안:** Resource 표(§6.3 Assets)에서 ID로 참조

### 4.2 컴포넌트 구조
> 컴포넌트 트리 · 각 컴포넌트 책임 · props/상태 경계(상태를 누가 소유하는가).

```
<RootComponent>
├─ <ChildA>        // 책임: <...>
│   └─ <LeafA>     // props: <...> / 상태 소유: 부모
└─ <ChildB>        // 책임: <...> / 로컬 상태: <...>
```

### 4.3 적용 디자인 패턴 (DSN)
> 프로젝트의 확립된 패턴에 매핑한 **설계 결정**. PLAN `traces`에서 `DSN-xxx`로 인용 가능(참조용, 커버리지 강제 아님).

| ID | 설계 결정 | 근거 (프로젝트 패턴 / 제약) |
|----|-----------|-----------------------------|
| DSN-001 | <상태는 기존 상태관리 패턴(Provider/스토어)을 재사용> | <프로젝트 상태관리 SSOT 규칙> |
| DSN-002 | <계산/판단 로직은 순수 함수/유틸로 분리> | <테스트 용이성·관심사 분리> |
| DSN-003 | <도메인 객체는 모델 클래스 + 직렬화로 표현> | <기존 모델 패턴> |

### 4.4 모듈/레이어 배치
- **배치:** <신규/수정 파일이 들어갈 디렉토리 · 기존 모듈/계층 구조와의 관계>
- **저장소/계층 선택:** <어떤 데이터를 어디에 두는가 + 선택 근거>

### 4.5 재사용 대상
- **재사용:** <기존 훅/유틸/컴포넌트 — §6.1 참고 코드와 교차 참조>
- **변경 최소화:** <기존 로직 변경 범위를 줄이는 방침>


## 5. Contract (파라미터 · 상태전이 · 세팅 키)
> 구현이 바뀌어도 유지돼야 하는 약속. 작은 기능이므로 표로 경량 기술.

### 5.1 핵심 파라미터 / 데이터
| ID | 항목 | 타입/범위 | 기본값 | 의미 |
|----|------|-----------|--------|------|
| CTR-001 | <param> | <number, 0..n> | <default> | <대상/단위> |

### 5.2 로컬 세팅 / 환경 키
| 키 | 타입 | 기본값 | 용도 |
|----|------|--------|------|
| `<settingKey>` | boolean/number | <default> | <덮어쓰기/오프셋 등> |

### 5.3 상태 전이 (성공/재시도 등)
| 현재 상태 | 조건 | 다음 상태 |
|-----------|------|-----------|
| <단계 A> | <성공> | <다음 단계> |
| <단계 A> | <미통과> | <재시도 / 이전 단계> |

## 6. Resources & References (착수 전 체크)
> 누락 시 작업이 막히는 것들. 시작 전 [ ] 점검.

### 6.1 참고 코드 / 재사용
- [ ] `<훅/유틸/컴포넌트 경로>` — <참고 이유/재사용 범위>
- [ ] 관심사 분리 규칙: <기존 로직 변경 최소화 방침>

### 6.2 외부 문서 (비즈니스 로직)
- [ ] `<doc 경로>` — <상세 워크플로/계산 로직 출처>

### 6.3 Assets (시안 등)
> 로컬 사본은 `<out>/assets/RES-ASSET-NNN_<slug>[_<nodeid>].<ext>` 로 저장하고, 아래 ID와 파일명을 일치시킨다(`references/output-location.md`).

| ID | 화면 | 참조 (url / node-id / assets 경로) | 비고 |
|----|------|------------------------------------|------|
| RES-ASSET-001 | <화면명> | <url / id> · `assets/RES-ASSET-001_<slug>.png` | |

### 6.4 API / Data (있다면)
| ID | 리소스 | 용도 | 상태 |
|----|--------|------|------|
| RES-API-001 | <엔드포인트> | <용도> | 준비/필요 |

## 7. Constraints
- **태스크 분리:** <PR 단위 분리 기준 — 상세: `references/task-pr-splitting.md`>
- **라우팅/구조:** <페이지 분리 / 화면 전환 규칙>
- **기술 제약:** <기존 로직 변경 최소화 등>

## 8. Edge Cases & Error Handling
| ID | 상황 | 기대 동작 |
|----|------|-----------|
| EDGE-001 | <진행 상태 인지> | <UI 피드백> |
| EDGE-002 | <처리 실패> | <모달(재시도/취소), N회 후 이전 화면> |

## 9. Testing Strategy
- **대상:** 계산/판단 로직(커스텀 훅·유틸) 위주
- **필수:** <핵심 비즈니스 로직 테스트 필수 범위>
- **추적:** 각 테스트는 `REQ-xxx`/`AC-xxx`/`CTR-xxx`에 매핑 — 상세: `references/traceability.md`
